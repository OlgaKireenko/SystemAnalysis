# 1. Сценарий и ресурсы API

## Пользовательский сценарий

Бизнес-пользователь банка работает с продуктовыми предложениями и использует продуктовый сервис для управления кредитными продуктами.

В рамках сценария пользователь может создавать кредитные продукты, просматривать существующие продукты, изменять их параметры, а также активировать и деактивировать продукты.

Каждое изменение кредитного продукта должно фиксироваться в истории изменений с указанием пользователя, даты и времени изменения.

## Ресурсы API

| Ресурс             | Назначение                                                                                                              | Source of Truth    |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------- | ------------------ |
| `CreditProduct`    | Хранение и управление актуальной информацией о кредитном продукте и его текущем статусе                                 | Продуктовый сервис |
| `ProductChangeLog` | Хранение истории изменений кредитных продуктов с информацией о выполненных изменениях, пользователе и времени изменения | Продуктовый сервис |


# 2-3 Задания находятся в документе по ссылке - 
## [Тут](https://docs.google.com/spreadsheets/d/1F0D8QvCNDxRTz-WjFamWa1nJeZVAlLKS8tSTz1W0Ak0/edit?usp=sharing)

# 4. Изменение ресурса

## Сценарий изменения

Бизнес-пользователь изменяет параметры существующего кредитного продукта, например, допустимый диапазоны суммы кредита, сроки или процентные ставки.

### API operation

**Method:** `PATCH`
**Path:** `/products/{productId}`

### Request

```json
{
  "loanAmounts": [
    {
      "min": 500000,
      "max": 1000000
    }
  ],
  "loanTerms": [
    {
      "min": 3,
      "max": 6,
      "interestRate": 21.9
    },
    {
      "min": 6,
      "max": 12,
      "interestRate": 20.9
    },
    {
      "min": 12,
      "max": 24,
      "interestRate": 19.9
    },
    {
      "min": 24,
      "max": 60,
      "interestRate": 18.9
    }
  ]
}
```

### Обоснование выбора PATCH

Используем `PATCH`, поскольку пользователь изменяет только отдельные параметры уже существующего кредитного продукта, а не заменяет весь ресурс целиком. `productId` передаётся в path и однозначно определяет изменяемый продукт. В request body передаются только те поля, которые необходимо изменить. `PUT` в данном случае менее удобен, поскольку предполагает полную замену ресурса. Отдельная command operation не требуется, так как изменение параметров является обычным частичным обновлением ресурса.

---

# Текстовый API-контракт

## Назначение операции

Изменение параметров существующего кредитного продукта.

## HTTP-метод и path

```text
PATCH /products/{productId}
```

## Path parameters

| Параметр    | Тип    | Обязательный | Ограничения              | Описание                           |
| ----------- | ------ | ------------ | ------------------------ | ---------------------------------- |
| `productId` | string | Да           | Уникальный идентификатор | Идентификатор изменяемого продукта |

## Headers

```text
Authorization: Bearer <token>
Content-Type: application/json
```

## Request body

| Поле                       | Тип           | Обязательное | Ограничения          | Описание                                   |
| -------------------------- | ------------- | ------------ | -------------------- | ------------------------------------------ |
| `loanAmount`               | array<object> | Нет          | Не менее 1 элемента  | Новый диапазон сумм кредита               |
| `loanAmount[].min`         | number        | Да           | > 0, `min < max`     | Минимальная сумма                          |
| `loanAmount[].max`         | number        | Да           | > `min`              | Максимальная сумма                         |
| `loanTerms`                | array<object> | Нет          | Не менее 1 элемента  | Новые диапазоны сроков и процентные ставки |
| `loanTerms[].min`          | number        | Да           | Целое число, > 0     | Минимальный срок в месяцах                 |
| `loanTerms[].max`          | number        | Да           | Целое число, > `min` | Максимальный срок в месяцах                |
| `loanTerms[].interestRate` | number        | Да           | > 0                  | Процентная ставка                          |

В `PATCH` все поля верхнего уровня request body являются необязательными, поскольку клиент может изменить только один параметр. Если передаётся `loanAmount` или `loanTerms`, обязательные поля соответствующих объектов должны быть заполнены.

## Успешный HTTP-status

```text
200 OK
```

## Response

```json
{
  "productId": "PL-001",
  "updatedAt": "2026-09-20T14:30:00Z"
}
```

## Response schema

| Поле        | Тип    | Обязательное | Ограничения              | Описание                           |
| ----------- | ------ | ------------ | ------------------------ | ---------------------------------- |
| `productId` | string | Да           | Уникальный идентификатор | Идентификатор изменённого продукта |
| `updatedAt` | string | Да           | ISO 8601                 | Дата и время последнего изменения  |



# 5. Текстовый API-контракт

## Операция: получение истории изменений кредитного продукта

### Назначение операции

Операция предназначена для получения истории изменений конкретного кредитного продукта. История позволяет определить, какие изменения выполнялись, когда они были выполнены и каким пользователем.

### HTTP-метод и path

```text
GET /productLogs
```

### Query-параметры

| Параметр    | Тип    | Обязательность | Ограничения                       | Описание                                                     |
| ----------- | ------ | -------------- | --------------------------------- | ------------------------------------------------------------ |
| `productId` | string | Да             | Уникальный идентификатор продукта | Идентификатор продукта, историю которого необходимо получить |
| `pageNum`   | number | Да             | Целое число, ≥ 1                  | Номер страницы                                               |
| `pageSize`  | number | Да             | Целое число, 1–100                | Количество записей на странице                               |
| `sortBy`    | string | Нет            | `changedAt`                       | Поле для сортировки                                          |
| `sortOrder` | string | Нет            | `asc`, `desc`                     | Направление сортировки                                       |

### Пример Request

```text
GET /productLogs?productId=PL-001&pageNum=1&pageSize=20&sortBy=changedAt&sortOrder=desc
```

### Headers

```text
Authorization: Bearer <token>
```

Request body отсутствует.

### Успешный HTTP-status

```text
200 OK
```

### Response

Сервер возвращает объект, содержащий массив записей историю изменений и информацию о пагинации.

### Схема Response

| Поле                 | Тип           | Обязательность | Ограничения                         | Описание                            |
| -------------------- | ------------- | -------------- | ----------------------------------- | ----------------------------------- |
| `items`              | array<object> | Да             | Может быть пустым                   | Список записей истории изменений    |
| `items[].logId`      | string        | Да             | Уникальный идентификатор            | Идентификатор записи истории        |
| `items[].productId`  | string        | Да             | Уникальный идентификатор продукта   | Идентификатор изменённого продукта  |
| `items[].changedBy`  | string        | Да             | Не пустое значение                  | Пользователь, выполнивший изменение |
| `items[].changedAt`  | string        | Да             | ISO 8601                            | Дата и время изменения              |
| `items[].changeType` | string        | Да             | `CREATE`, `UPDATE`, `STATUS_CHANGE` | Тип изменения                       |
| `items[].changes`    | object        | Да             | Может содержать изменённые поля     | Информация о выполненных изменениях |
| `pageNum`            | number        | Да             | Целое число, ≥ 1                    | Номер текущей страницы              |
| `pageSize`           | number        | Да             | Целое число, 1–100                  | Размер страницы                     |
| `totalItems`         | number        | Да             | Целое число, ≥ 0                    | Общее количество записей            |
| `totalPages`         | number        | Да             | Целое число, ≥ 0                    | Общее количество страниц            |

### Пример Response

```json
{
  "items": [
    {
      "logId": "LOG-001",
      "productId": "PL-001",
      "changedBy": "user123",
      "changedAt": "2026-09-20T14:30:00Z",
      "changeType": "UPDATE",
      "changes": {
        "loanAmount": {
          "oldValue": 10000000,
          "newValue": 15000000
        }
      }
    },
    {
      "logId": "LOG-002",
      "productId": "PL-001",
      "changedBy": "user456",
      "changedAt": "2026-09-19T11:15:00Z",
      "changeType": "STATUS_CHANGE",
      "changes": {
        "active": {
          "oldValue": false,
          "newValue": true
        }
      }
    }
  ],
  "pageNum": 1,
  "pageSize": 20,
  "totalItems": 2,
  "totalPages": 1
}
```




